// 12. Escribir un programa que calcule el área de un rectángulo:

let base = Number(prompt("ingrese la base del triangulo"));
let altura = Number(prompt("ingrese la altura del triagulo"));
let area = base * altura;
alert("la base del triangulo es: "+area)

